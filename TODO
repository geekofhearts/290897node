 * finish operator definitions for numbers, strings and objects, plus conversion function syntax.
 * code tree generation (and as-string export)
 * write test script for sqrt
 * think about npm, maybe.
